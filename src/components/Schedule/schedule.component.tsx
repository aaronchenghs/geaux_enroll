import React, { Fragment } from "react";
import styles from "./schedule.module.scss";

const Schedule = (): JSX.Element => {
  return (
    <Fragment>
      <div className={styles.Schedule}>This is the Schedule view</div>
    </Fragment>
  );
};
export default Schedule;
